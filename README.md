# php-alpine-oci8

php-fpm version 8 with Oracle Database client Support 
test with x86_64 CPU and M2 arm base CPU 

support php 8.x and 7.x build image example

docker build . --build-arg VERSION=8.1 -t php:8.1-fpm-alpine-oci8 --progress=plain

